import { SelectResultDirective } from './select-result.directive';

describe('SelectResultDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectResultDirective();
    expect(directive).toBeTruthy();
  });
});
