import { InputFillDirective } from './input-fill.directive';

describe('InputFillDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFillDirective();
    expect(directive).toBeTruthy();
  });
});
